V102 – Resume Diagnostics

Changes made:
- Added lightweight resume diagnostics inside index.html.
- Logs focus, visibilitychange, pageshow, resume accepted/blocked decisions, layout refresh scheduling/runs, render starts, and canvas integrity failures.
- Added window.KennelResumeDiagnostics helper:
  - KennelResumeDiagnostics.read()
  - KennelResumeDiagnostics.print()
  - KennelResumeDiagnostics.clear()
- Diagnostic log is capped to the latest 120 entries under localStorage key: kennel_resume_diagnostics_v102.
- Updated service worker cache name to kennel-manager-v102-resume-diagnostics so the new index.html is picked up cleanly.

Audit:
- No UI changes.
- No kennel layout changes.
- No localStorage data migration changes.
- No app behavior changes intended beyond diagnostic logging.
- Syntax checked extracted inline JavaScript with node --check.
